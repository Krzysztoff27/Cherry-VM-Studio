services:
  api:
    container_name: cherry-api
    restart: always
    image: cherry-api
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - ALGORITHM=${ALGORITHM}
      - ACCESS_TOKEN_EXPIRE_MINUTES=${ACCESS_TOKEN_EXPIRE_MINUTES}
      - ACCESS_GROUP_GID=${ACCESS_GROUP_GID}
    ports:
      - "8000:8000"
    volumes:
      - "/opt/cherry-vm-manager/auth/:/api/data/auth/:ro"
    networks:
      cvmm-internal:
  
networks:
  cvmm-internal:
    name: cvmm-internal
    external: true
