polkit.addRule(function(action, subject){
    if ((action.id == "com.cvms.stack") && ((subject.isInGroup == "CherryWorker") && (subject.user == "CherryWorker"))){
        return polkit.Result.YES;
    }
});