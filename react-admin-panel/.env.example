# create .env.development.local with development variables 
# and .env.production.local with production variables
# based on this template:
VITE_API_BASE_URL=https://api.example.com                   # must NOT end with "/"
VITE_API_WEBSOCKET_URL=ws://api.example.com:8000            # must NOT end with "/"
VITE_TRAEFIK_PANEL_URL=http://traefik.example.com:8080      # must NOT end with "/"
VITE_GUACAMOLE_PANEL_URL=http://guacamole.example.com:5900   # must NOT end with "/"